query getProfile($userID: Int!) {
    getProfileInfo(userId: $userID) {
        profile {
            createdAt
            id
            firstName
            lastName
            userName
            avatars {
                url
                height
                width
            }
        }
    }
}