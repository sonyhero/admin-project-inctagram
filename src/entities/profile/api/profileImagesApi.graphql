query getProfilePostImages($userId: Int!) {
    getProfileInfo(userId: $userId) {
      posts {
          height
          width
          fileSize
          url
          id
          createdAt
      }
    }
}
